CREATE TABLE IF NOT EXISTS RESERVE_MEETING (
	RECEPTION_NO Integer IDENTITY PRIMARY KEY,
	REQUEST_DATE1 TIMESTAMP ,
	REQUEST_DATE2 TIMESTAMP,
	NAME VARCHAR(100),
	SCHOOL VARCHAR(100),
	DEV BOOLEAN,
	MACHING_POINT  VARCHAR(300),
	CREATE_DATE TIMESTAMP,
	UPDATE_DATE TIMESTAMP

);

CREATE TABLE IF NOT EXISTS UNIVERSITY (
	category VARCHAR(10),
	university VARCHAR(100)

);
